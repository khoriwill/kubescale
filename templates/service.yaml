apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubescale.fullname" . }}
  labels:
    {{- include "kubescale.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubescale.selectorLabels" . | nindent 4 }}
